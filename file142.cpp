// linked list in STL
// traversal in list is slower than vector, but insertion and deletion is faster than vector
// We cannot perform binary search on a linked lsit(because from a node we can either go to next node or the previous node)


#include <iostream>
#include <list>

using namespace std;

int main(){
    list<int> ll;
    
}